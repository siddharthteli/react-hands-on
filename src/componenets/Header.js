//This can contain class or just a function.

const Header = (title) => { 
    return(
    <header>
        <h1>This is our header {title.title}.</h1>
    </header>
    )
       
};

export default Header